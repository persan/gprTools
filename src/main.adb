with Ada.Integer_Text_IO; use Ada.Integer_Text_IO;
with Ada.Text_IO; use Ada.Text_IO;
procedure Main is
   Num : constant := 16#12343#;
   I   : array 1 .. 2 of INTEGER;
begin
   Put (num, Base => 2); new_line;
   Put (20, Base => 8); new_line;
   Put (num, Base => 10); new_line;
   Put (20, Base => 16); New_Line;
   for IX of I loop

end;
